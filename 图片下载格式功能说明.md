# 图片下载格式功能说明

## 功能概述

为无限画布项目添加了 **图片下载格式选择** 功能，用户可以在配置面板中选择下载图片时的保存格式。

## 功能特性

### 1. 格式选项

- **原始格式**（默认）：保持 API 返回的原始格式（PNG/JPG/GIF）
- **WebP**：自动转换为 WebP 格式，体积减少 25-35%

### 2. 配置位置

**路径**：设置 → 偏好设置 → 图片下载格式

**默认值**：`原始格式`（不影响现有用户的使用习惯）

### 3. 应用范围

转换功能应用于所有图片下载场景：

✅ **画布页面**（`/canvas/project`）
   - 单个节点图片下载
   - 批量图片下载

✅ **生图工作台**（`/image`）
   - 生成结果图片下载

✅ **资产页面**（`/assets`）
   - 资产图片下载

## 技术实现

### 1. 核心转换逻辑

**文件**：`web/src/lib/image-format-converter.ts`

```typescript
// 主函数
prepareImageForDownload(dataUrl: string, format: "original" | "webp")
  → Promise<{ dataUrl: string; extension: string }>

// 功能：
// - format = "original": 保持原格式，返回原始 dataUrl
// - format = "webp": 使用 Canvas API 转换为 WebP
```

**转换细节**：
- 使用 HTML5 Canvas API 进行格式转换
- WebP 质量设置为 **95%**（接近无损）
- 保持原始图片尺寸不变
- 自动识别原始格式扩展名（png/jpg/webp/gif）

### 2. 配置存储

**文件**：`web/src/stores/use-config-store.ts`

```typescript
export type AiConfig = {
    // ... 其他配置
    imageDownloadFormat: "original" | "webp";  // 新增
}

export const defaultConfig: AiConfig = {
    // ... 其他默认值
    imageDownloadFormat: "original",  // 默认原始格式
};
```

### 3. UI 配置组件

**文件**：`web/src/components/layout/app-config-modal.tsx`

在"偏好设置"标签页的"生成偏好"区域添加了下拉选择器：

```tsx
<ProFormSelect
    name="imageDownloadFormat"
    label={t("config.preferences.imageDownloadFormat")}
    tooltip={t("config.preferences.imageDownloadFormatDescription")}
    options={[
        { label: t("config.preferences.originalFormat"), value: "original" },
        { label: "WebP", value: "webp" },
    ]}
/>
```

### 4. 下载函数修改

所有下载函数都已更新，调用格式转换函数：

**生图工作台**（`web/src/pages/image/index.tsx`）：
```typescript
const downloadImage = async (image: GeneratedImage, index: number) => {
    const format = useConfigStore.getState().config.imageDownloadFormat;
    const { dataUrl, extension } = await prepareImageForDownload(image.dataUrl, format);
    saveAs(dataUrl, `image-${index + 1}.${extension}`);
};
```

**画布页面**（`web/src/pages/canvas/project.tsx`）：
```typescript
const handleDownload = async (node: CanvasImageNode) => {
    const format = useConfigStore.getState().config.imageDownloadFormat;
    const { dataUrl, extension } = await prepareImageForDownload(node.dataUrl, format);
    saveAs(dataUrl, `${node.name}.${extension}`);
};
```

**资产页面**（`web/src/pages/assets/index.tsx`）：
```typescript
const downloadAsset = async (asset: Asset) => {
    const format = useConfigStore.getState().config.imageDownloadFormat;
    const { dataUrl, extension } = await prepareImageForDownload(asset.content, format);
    saveAs(dataUrl, `${asset.title}.${extension}`);
};
```

### 5. 国际化翻译

**中文**（`web/src/i18n/locales/zh-CN.ts`）：
```typescript
preferences: {
    imageDownloadFormat: "图片下载格式",
    imageDownloadFormatDescription: "下载图片时的保存格式，WebP 体积更小。",
    originalFormat: "原始格式",
    // ...
}
```

**英文**（`web/src/i18n/locales/en-US.ts`）：
```typescript
preferences: {
    imageDownloadFormat: "Image download format",
    imageDownloadFormatDescription: "Save format when downloading images. WebP has smaller file size.",
    originalFormat: "Original format",
    // ...
}
```

## 测试建议

### 测试步骤

1. **配置测试**
   - 打开配置面板 → 偏好设置
   - 找到"图片下载格式"选项
   - 验证默认值为"原始格式"
   - 切换到"WebP"并保存

2. **生图工作台测试**
   - 前往生图工作台（`/image`）
   - 输入提示词生成图片
   - 点击下载按钮
   - 验证下载的文件扩展名是 `.webp`

3. **画布测试**
   - 前往画布页面（`/canvas`）
   - 创建图片节点
   - 右键点击图片节点
   - 选择"下载"
   - 验证下载的文件格式

4. **资产测试**
   - 前往资产页面（`/assets`）
   - 选择一个图片资产
   - 点击下载按钮
   - 验证下载的文件格式

5. **格式切换测试**
   - 下载一张图片（WebP格式）
   - 切换配置为"原始格式"
   - 下载同一张图片
   - 对比文件大小（原始格式应该更大）

### 预期结果

- ✅ 配置选项显示正常
- ✅ WebP 格式下载的文件体积显著减小（约 25-35%）
- ✅ 原始格式下载的文件与 API 返回格式一致
- ✅ 图片质量无明显损失
- ✅ 所有下载场景都应用配置

## WebP 格式优势

- **体积优势**：比 PNG 小 25-35%，比 JPEG 小 30-50%
- **质量保持**：95% 质量参数接近无损
- **浏览器支持**：现代浏览器均支持（Chrome 23+, Firefox 65+, Safari 14+）
- **应用场景**：适合需要分享、上传或存储大量图片的场景

## 注意事项

1. **兼容性**：WebP 格式在一些旧版软件中可能无法打开
2. **转换时间**：大尺寸图片转换需要几秒钟
3. **质量参数**：当前固定为 95%，后续可考虑设为可配置项
4. **原始格式优先**：默认值设为"原始格式"，不影响用户现有习惯

## 修改的文件清单

1. ✅ `web/src/stores/use-config-store.ts` - 添加配置项
2. ✅ `web/src/lib/image-format-converter.ts` - 新建转换逻辑
3. ✅ `web/src/components/layout/app-config-modal.tsx` - UI 配置组件
4. ✅ `web/src/pages/image/index.tsx` - 生图工作台下载
5. ✅ `web/src/pages/canvas/project.tsx` - 画布页面下载
6. ✅ `web/src/pages/assets/index.tsx` - 资产页面下载
7. ✅ `web/src/i18n/locales/zh-CN.ts` - 中文翻译
8. ✅ `web/src/i18n/locales/en-US.ts` - 英文翻译

## 构建状态

✅ **构建成功** - `npm run build` 通过，无错误

---

**开发时间**：2026-08-12  
**功能版本**：v1.0

# 无限画布 - 小白完整使用教程

欢迎使用无限画布！这是一款面向图片创作的开源工作台，让你可以在一个无限的画布上自由创作、编排和生成 AI 图片。本教程将手把手教你如何从零开始使用无限画布。

---

## 📋 目录

1. [什么是无限画布](#什么是无限画布)
2. [如何访问无限画布](#如何访问无限画布)
3. [首次使用必看：配置 AI 接口](#首次使用必看配置-ai-接口)
4. [画布基础操作](#画布基础操作)
5. [创建你的第一个 AI 图片](#创建你的第一个-ai-图片)
6. [三种节点详解](#三种节点详解)
7. [画布助手使用](#画布助手使用)
8. [提示词库使用](#提示词库使用)
9. [我的素材管理](#我的素材管理)
10. [高级功能](#高级功能)
11. [常见问题](#常见问题)

---

## 什么是无限画布

无限画布是一个在线的 AI 图片创作工具，它把以下功能整合在一起：

- 📐 **无限画布**：像白板一样自由摆放图片、文字、连线
- 🎨 **AI 图片生成**：支持文生图、图生图、参考图编辑
- 💬 **对话助手**：可以和 AI 聊天、生成文字、生成图片
- 📚 **提示词库**：内置大量优质提示词，开箱即用
- 🎥 **视频/音频生成**：支持 AI 视频和音频创作
- 💾 **素材沉淀**：把好用的提示词和图片保存下来反复使用

**适合谁用？**
- 设计师：快速生成设计灵感和参考图
- 自媒体创作者：批量生成配图和封面
- 产品经理：制作产品原型和视觉方案
- AI 爱好者：探索 AI 图片生成的各种可能性

---

## 如何访问无限画布

无限画布提供多种访问方式：

### 方式一：在线访问（推荐新手）

直接访问已部署的网站（你自己的或公开的）：
- 打开浏览器
- 输入网址（例如：`https://img.panlai.me`）
- 无需注册账号，立即开始使用

### 方式二：本地运行（开发者）

如果你想在本地电脑运行：

```bash
# 1. 克隆项目
git clone https://github.com/basketikun/infinite-canvas.git

# 2. 进入 web 目录
cd infinite-canvas/web

# 3. 安装依赖（需要先安装 Bun）
bun install

# 4. 启动开发服务器
bun run dev

# 5. 打开浏览器访问
# http://localhost:3000
```

### 方式三：Docker 运行（最简单）

如果你熟悉 Docker：

```bash
# 克隆项目
git clone https://github.com/basketikun/infinite-canvas.git
cd infinite-canvas

# 启动
docker compose up -d

# 访问 http://localhost:3000
```

---

## 首次使用必看：配置 AI 接口

⚠️ **重要**：无限画布本身不提供 AI 服务，你需要配置自己的 AI API 才能使用生成功能。

### 第一步：获取 AI API

你需要一个兼容 OpenAI 格式的 API 服务，可以选择：

**官方服务：**
- OpenAI（ChatGPT）：https://platform.openai.com
- Google Gemini：https://ai.google.dev
- Anthropic Claude：https://console.anthropic.com

**第三方中转服务（国内访问友好）：**
- Infistar.ai 无限星河（推荐）
- Atlas Cloud
- 秘塔科技（MiniMax H3）
- 其他 OpenAI API 中转服务

### 第二步：配置到无限画布

1. **打开配置面板**
   - 点击页面右上角的 ⚙️ **设置图标**
   - 或按快捷键 `Ctrl + ,`（Windows）/ `Cmd + ,`（Mac）

2. **添加渠道**
   - 点击"渠道配置"标签
   - 点击 **"+ 添加渠道"** 按钮

3. **填写配置信息**
   - **渠道名称**：给这个 API 起个名字（例如："我的 OpenAI"）
   - **协议类型**：选择你的 API 类型
     - `OpenAI`：适用于 OpenAI 和大多数兼容服务
     - `Gemini`：适用于 Google Gemini
     - `Grok`：适用于 xAI Grok
     - `Seedance`：适用于 Seedance 图片生成
   - **接口地址 (Base URL)**：
     - OpenAI 官方：`https://api.openai.com`
     - 中转服务：填写服务商提供的地址（例如：`https://api.example.com`）
   - **API Key**：粘贴你的 API 密钥（通常以 `sk-` 开头）

4. **选择模型**
   - 点击 **"选择模型"** 按钮
   - 系统会自动拉取该 API 支持的模型列表
   - 勾选你想使用的模型（例如：`gpt-4o`、`gemini-2.0-flash-exp`）
   - 为每个模型选择能力类型：
     - `图片`：图片生成模型
     - `文本`：文字对话模型
     - `视频`：视频生成模型
     - `音频`：音频生成模型

5. **设置默认模型**
   - 在"偏好设置"标签中
   - 设置默认图片模型（用于生图）
   - 设置默认文本模型（用于对话）
   - 设置默认视频模型（用于视频生成）

6. **保存配置**
   - 点击 **"保存"** 按钮
   - 配置会保存在浏览器本地

### 配置示例

**示例 1：配置 OpenAI**
```
渠道名称：OpenAI 官方
协议类型：OpenAI
接口地址：https://api.openai.com
API Key：sk-xxxxxxxxxxxxxxxxxxxxxxxx
模型：gpt-4o (文本), dall-e-3 (图片)
```

**示例 2：配置 Gemini**
```
渠道名称：Google Gemini
协议类型：Gemini
接口地址：https://generativelanguage.googleapis.com
API Key：AIzaSyxxxxxxxxxxxxxxxxxxxxxxxx
模型：gemini-2.0-flash-exp (文本+图片)
```

---

## 画布基础操作

### 创建画布项目

1. 点击左上角 **"画布项目"**
2. 点击 **"+ 新建画布"**
3. 输入项目名称（例如："我的第一个作品"）
4. 点击新创建的项目进入画布

### 画布操作

**移动画布**
- 按住 **空格键 + 鼠标左键拖动**
- 或者直接 **鼠标中键拖动**

**缩放画布**
- **滚轮向上/向下**：放大/缩小
- **Ctrl + 滚轮**：精细缩放
- 或使用右下角的 **缩放滑杆**

**重置视图**
- 点击右下角的 **"重置视图"** 按钮
- 或按 **`Ctrl + 0`**

**背景设置**
- 点击顶部工具栏的 **背景按钮**
- 选择：点阵 / 网格 / 空白

**主题切换**
- 点击右上角的 **🌙 月亮图标** / **☀️ 太阳图标**
- 切换浅色/深色主题

### 小地图

- 点击右下角的 **小地图图标** 打开/关闭
- 小地图显示整个画布的缩略图
- 拖动小地图中的方框快速导航

---

## 创建你的第一个 AI 图片

让我们一步步创建第一张 AI 图片：

### 方法一：快速生成（最简单）

1. **新建文本节点**
   - 点击顶部工具栏的 **"T 文本"** 按钮
   - 或按快捷键 **`T`**
   - 在画布上点击一个位置，会创建一个空文本节点

2. **输入提示词**
   - 双击文本节点
   - 输入你的提示词，例如：
     ```
     一只可爱的橘猫，坐在窗台上，阳光洒在身上，温暖的午后
     ```
   - 点击节点外部区域完成编辑

3. **生成图片**
   - 选中文本节点（点击节点边框）
   - 点击顶部工具栏的 **"生图"** 按钮
   - 系统会自动创建一个生成配置节点，并开始生成

4. **查看结果**
   - 稍等片刻，图片生成完成
   - 生成的图片会出现在生成配置节点的右侧
   - 如果生成多张，会显示为图片组，点击可展开查看

### 方法二：使用画布助手

1. **打开助手面板**
   - 点击画布右侧的 **助手图标**
   - 或者它已经默认打开了

2. **输入提示词**
   - 在助手输入框中输入：
     ```
     画一只在雪地里玩耍的哈士奇
     ```

3. **生成图片**
   - 点击输入框右侧的 **"生图"** 图标
   - 或直接发送，然后在回复中点击"插入画布"

4. **插入画布**
   - 生成完成后，点击图片下方的 **"插入画布"** 按钮
   - 图片会自动添加到画布中心

---

## 三种节点详解

无限画布中有三种核心节点类型：

### 1. 文本节点 📝

**用途**：保存提示词、文字说明、AI 生成的文字

**创建方式**：
- 点击顶部 **"T 文本"** 按钮
- 快捷键 **`T`**

**操作**：
- **编辑文字**：双击节点内容区域
- **调整字号**：选中节点后，用顶部工具栏的 **缩小/放大** 按钮
- **生成文本**：选中节点 → 点击工具栏 **"编辑"** → 在下方对话框输入指令
- **生成图片**：选中节点 → 点击工具栏 **"生图"**

**示例场景**：
```
节点 1：原始想法
"画一只猫"

↓ 点击"编辑"，输入"帮我扩写这个提示词，增加细节"

节点 2：AI 扩写后
"一只优雅的波斯猫，蓝色的眼睛，雪白的长毛，
坐在红色天鹅绒垫子上，背景是欧式装修的客厅"

↓ 点击"生图"

节点 3：生成配置 → 节点 4：生成的图片
```

### 2. 图片节点 🖼️

**用途**：展示上传的图片、AI 生成的图片、素材库的图片

**创建方式**：
- 点击顶部 **"上传图片"** 按钮
- 直接 **拖拽图片文件** 到画布
- 从助手或生成结果插入

**操作**：
- **替换图片**：选中节点 → 点击 **"替换"** → 选择新图片
- **下载图片**：选中节点 → 点击 **"下载"**
- **保存到素材**：选中节点 → 点击 **"保存到我的素材"**
- **裁剪图片**：选中节点 → 点击 **"裁剪"** → 拖动裁剪框 → 确认
- **图片变换**：选中节点 → 点击 **"变换"** → 选择变换类型
- **图生图**：选中节点 → 在下方对话框输入修改指令
- **缩放模式**：选中节点 → 点击 **锁/解锁图标** 切换等比/自由缩放

**图生图示例**：
```
原始图片：一只坐着的猫
↓
在下方对话框输入："把猫改成站立的姿势"
↓
生成新图片节点：一只站立的猫（自动连线）
```

### 3. 生成配置节点 ⚙️

**用途**：汇总上游内容，批量生成图片或文本

**创建方式**：
- 从文本节点点击 **"生图"** 自动创建
- 或手动点击顶部 **"生成配置"** 按钮

**功能**：
- **读取上游**：自动读取所有连接的文本节点和图片节点
- **模式切换**：
  - `图片` 模式：生成图片
  - `文本` 模式：生成文字对话
  - `视频` 模式：生成视频
  - `音频` 模式：生成音频
- **配置参数**：
  - 选择模型
  - 设置图片比例（1:1, 16:9, 9:16 等）
  - 设置生成数量（1-10 张）
  - 调整图片质量
- **预览输入**：点击 **"预览"** 查看将要发送给 AI 的内容
- **调整顺序**：拖动预览中的内容调整发送顺序

**批量生成示例**：
```
文本节点 1："一只猫"    ↘
文本节点 2："赛博朋克风格" → 生成配置节点
图片节点 3：参考图片      ↗     ↓
                                设置数量：4 张
                                设置比例：1:1
                                ↓
                            同时生成 4 张图片
```

---

## 画布助手使用

画布助手是右侧的 AI 对话面板，可以帮你生成文字和图片。

### 基础对话

1. **打开助手**：点击右侧的助手图标（默认已打开）
2. **输入问题**：在输入框输入任何问题
   ```
   如何写一个好的 AI 绘画提示词？
   ```
3. **发送**：按 **Enter** 或点击 **发送按钮**
4. **查看回复**：AI 会生成文字回复

### 生成图片

1. **输入提示词**：
   ```
   画一个未来科技感的城市天际线
   ```
2. **点击生图图标**：输入框右侧的图片图标
3. **等待生成**：图片会显示在对话中
4. **插入画布**：点击图片下方的 **"插入画布"** 按钮

### 引用画布节点

**场景**：让助手基于画布中已有的内容继续创作

1. **选中节点**：在画布中选中一个或多个节点
2. **点击"编辑"**：顶部工具栏的编辑按钮
3. **自动引用**：助手会自动引用选中的节点和它们的上游节点
4. **输入指令**：
   ```
   基于这张图片，生成一个相似风格的夜景版本
   ```
5. **发送**：AI 会理解引用的内容并生成结果

### @ 引用功能

在输入框中可以 **@** 引用已连接的资源：

1. **输入 @**：在输入框中输入 `@`
2. **选择资源**：会弹出已连接的图片、文本、视频列表
3. **选择后**：
   - 图片会显示缩略图
   - 文本会显示内容预览
4. **发送**：AI 会理解你引用的内容

**示例**：
```
@图片1 这张图的风格是什么？帮我生成一张类似风格的图
```

### 历史会话

- **查看历史**：助手会保留所有对话历史
- **重试回答**：点击回复右侧的 **"重试"** 图标
- **删除会话**：点击回复右侧的 **"删除"** 图标
- **插入文本**：对于文字回复，可以点击 **"插入画布"** 变成文本节点

---

## 提示词库使用

提示词库内置了大量优质的 AI 绘画提示词，可以直接使用。

### 打开提示词库

1. 点击顶部导航栏的 **"提示词库"**
2. 或访问 `/prompts` 页面

### 浏览提示词

- **首次加载**：提示词会从 GitHub 仓库自动拉取（需要联网）
- **缓存**：拉取后会缓存到浏览器，下次秒开
- **筛选**：
  - 按 **来源** 筛选（不同的提示词仓库）
  - 按 **标签** 筛选（风格、主题等）
  - **搜索** 输入关键词

### 使用提示词

1. **找到喜欢的提示词**：浏览或搜索
2. **查看详情**：点击提示词卡片
3. **使用方式**：
   - **复制**：点击 **"复制"** 按钮，粘贴到画布节点
   - **加入素材**：点击 **"加入我的素材"**，永久保存
   - **查看示例图**：查看这个提示词生成的效果

### 示例

```
找到提示词："赛博朋克风格城市"
↓
点击"复制"
↓
在画布创建文本节点
↓
粘贴提示词
↓
点击"生图"
```

---

## 我的素材管理

"我的素材"是你的私人素材库，可以保存常用的提示词和图片。

### 打开素材库

1. 点击顶部导航栏的 **"我的素材"**
2. 或访问 `/materials` 页面

### 添加素材

**方式一：从提示词库添加**
- 在提示词库中点击 **"加入我的素材"**

**方式二：从画布节点添加**
- 选中图片节点 → 点击 **"保存到我的素材"**

**方式三：手动创建**
1. 点击右上角 **"+ 新建素材"**
2. 选择类型：文本 / 图片
3. 填写信息：
   - 标题：给素材起个名字
   - 内容：填写提示词或上传图片
   - 标签：添加标签方便分类（可选）
   - 来源：记录来源（可选）
   - 备注：添加说明（可选）
4. 点击 **"保存"**

### 使用素材

1. **浏览素材**：
   - 所有素材以卡片形式展示
   - 图片素材显示缩略图
   - 文本素材显示部分内容

2. **筛选素材**：
   - 按类型筛选：全部 / 文本 / 图片
   - 搜索：输入关键词
   - 分页浏览

3. **操作素材**：
   - **编辑**：点击素材 → 点击 **"编辑"** → 修改内容
   - **复制**：点击 **"复制"** 按钮（文本素材）
   - **下载**：点击 **"下载"** 按钮（图片素材）
   - **插入画布**：点击 **"插入画布"** → 选择目标画布项目
   - **删除**：点击 **"删除"** 按钮

---

## 高级功能

### 节点连线

连线用于建立节点之间的逻辑关系。

**创建连线**：
1. 鼠标悬停到节点边缘
2. 会出现 **左右两个连接点**
3. 从右侧连接点拖出线
4. 连接到另一个节点的左侧连接点

**连线的作用**：
- 文本节点 → 生成配置节点：提供提示词
- 图片节点 → 生成配置节点：提供参考图
- 节点 A → 节点 B：表示 B 是基于 A 生成的

**删除连线**：
- 选中连线（点击连线）
- 按 **Delete** 键
- 或右键 → 删除

### 框选和多选

**框选**：
- 按住 **Shift** + 鼠标拖动
- 框选区域内的所有节点

**多选**：
- 按住 **Ctrl**（Windows）/ **Cmd**（Mac）
- 依次点击多个节点

**全选**：
- 按 **Ctrl + A**（Windows）/ **Cmd + A**（Mac）

**批量操作**：
- 选中多个节点后可以：
  - 一起移动
  - 一起删除
  - 一起复制粘贴
  - 一起调整大小

### 复制粘贴

**复制节点**：
- 选中节点
- 按 **Ctrl + C**（Windows）/ **Cmd + C**（Mac）

**粘贴节点**：
- 按 **Ctrl + V**（Windows）/ **Cmd + V**（Mac）
- 粘贴的节点会出现在鼠标位置

**复制会保留**：
- 节点内容（文字、图片）
- 节点大小和样式
- 节点之间的连线（如果同时复制了多个相连的节点）

### 撤销和重做

- **撤销**：**Ctrl + Z**（Windows）/ **Cmd + Z**（Mac）
- **重做**：**Ctrl + Shift + Z**（Windows）/ **Cmd + Shift + Z**（Mac）

支持撤销的操作：
- 创建/删除节点
- 移动节点
- 编辑内容
- 创建/删除连线
- 画布视图变化

### 导入导出画布

**导出画布**：
1. 打开画布项目列表
2. 找到要导出的项目
3. 点击项目右侧的 **"..."** 菜单
4. 点击 **"导出"**
5. 会下载一个 JSON 文件

**导入画布**：
1. 打开画布项目列表
2. 点击 **"导入画布"** 按钮
3. 选择之前导出的 JSON 文件
4. 画布会被还原，包括所有节点和连线

**用途**：
- 备份重要项目
- 在不同设备之间迁移
- 分享画布给其他人

### 自定义生图脚本

如果你的 API 接口调用方式比较特殊，可以自定义生图脚本。

1. **打开渠道配置**：设置 → 渠道配置
2. **编辑渠道**：选择一个渠道
3. **选择模型**：找到要自定义的模型
4. **点击"脚本"按钮**
5. **编写脚本**：使用 JavaScript 自定义请求
6. **保存**

**示例**：自定义图片生成请求
```javascript
async function generate(params) {
  const { prompt, width, height, model, apiKey, baseUrl } = params;
  
  const response = await fetch(`${baseUrl}/custom/generate`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${apiKey}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      text: prompt,
      size: `${width}x${height}`,
      model: model
    })
  });
  
  const data = await response.json();
  return data.image_url;
}
```

---

## 常见问题

### Q1: 为什么点击"生图"没有反应？

**可能原因**：
1. **没有配置 API**：检查右上角设置中是否填写了 Base URL 和 API Key
2. **没有选择模型**：检查是否拉取了模型列表，并设置了默认图片模型
3. **API 配额用完**：检查你的 API 服务是否还有余额
4. **网络问题**：检查浏览器控制台是否有网络错误

**解决方案**：
- 打开浏览器开发者工具（F12）
- 查看 Console 标签中的错误信息
- 根据错误信息调整配置

### Q2: 生成的图片质量不好怎么办？

**提示词优化**：
- 使用更详细的描述
- 添加风格关键词（如"高质量"、"4K"、"精细"）
- 参考提示词库中的优质示例
- 使用英文提示词通常效果更好

**模型选择**：
- 尝试更强大的模型（如 DALL-E 3、Midjourney）
- 在生成配置节点中调整图片质量设置

### Q3: 如何批量生成多张相似的图片？

1. 创建一个文本节点，写好提示词
2. 点击"生图"创建生成配置节点
3. 在生成配置节点中设置 **数量：4**（或更多）
4. 点击生成，会同时生成多张图片
5. 生成的图片会以图片组形式展示，点击可展开查看所有结果

### Q4: 提示词库无法加载

**原因**：提示词库从 GitHub 拉取，可能受网络影响

**解决方案**：
- 检查网络连接
- 使用代理或 VPN
- 稍后再试（拉取后会缓存到本地）
- 或者直接使用"我的素材"保存自己的提示词

### Q5: 画布数据会丢失吗？

**数据保存位置**：
- 画布项目、素材、配置都保存在 **浏览器本地存储**
- 不会上传到服务器

**注意事项**：
- ⚠️ 清除浏览器数据会导致丢失
- ⚠️ 换浏览器或换电脑无法同步
- ⚠️ 隐私模式/无痕模式下数据不会保存

**备份方案**：
- 定期导出重要画布项目（JSON 文件）
- 重要素材保存到外部（复制文字、下载图片）
- 使用 WebDAV 同步功能（高级功能，需自行配置）

### Q6: 图片生成很慢

**正常情况**：
- AI 图片生成通常需要 10-60 秒
- 视频生成可能需要几分钟

**加速方法**：
- 使用更快的模型
- 选择较小的图片尺寸
- 使用国内中转服务减少网络延迟

### Q7: 如何生成特定风格的图片？

**风格关键词**：
在提示词中加入风格描述：
- **写实风格**："photorealistic", "ultra realistic", "8k photograph"
- **动漫风格**："anime style", "manga", "Studio Ghibli style"
- **油画风格**："oil painting", "classical art", "renaissance style"
- **水彩风格**："watercolor painting", "soft colors"
- **赛博朋克**："cyberpunk", "neon lights", "futuristic"
- **中国风**："Chinese ink painting", "traditional Chinese style"

**使用参考图**：
- 上传一张风格参考图
- 连接到生成配置节点
- AI 会参考图片的风格

### Q8: 画布操作卡顿

**优化方法**：
- 关闭小地图（右下角）
- 减少画布中的节点数量
- 导出旧画布，创建新画布
- 使用更好的浏览器（推荐 Chrome）
- 关闭浏览器中的其他标签页

### Q9: API Key 安全吗？

**存储方式**：
- API Key 只保存在 **你的浏览器本地**
- 不会上传到无限画布的服务器

**安全建议**：
- 不要在公共电脑上保存 API Key
- 定期更换 API Key
- 使用权限受限的 API Key（如果服务商支持）
- 不要截图分享包含 API Key 的配置页面

### Q10: 如何联系作者或获取帮助？

**官方渠道**：
- GitHub 项目：https://github.com/basketikun/infinite-canvas
- 作者邮箱：1844025705@qq.com
- QQ：1844025705
- QQ 群：【AI开源交流】https://qm.qq.com/q/DFnKzZ807u
- Linux.do 社区：https://linux.do/

---

## 快捷键参考

| 功能 | Windows/Linux | Mac |
|------|--------------|-----|
| 创建文本节点 | `T` | `T` |
| 移动画布 | `空格 + 拖动` | `空格 + 拖动` |
| 框选 | `Shift + 拖动` | `Shift + 拖动` |
| 多选 | `Ctrl + 点击` | `Cmd + 点击` |
| 全选 | `Ctrl + A` | `Cmd + A` |
| 复制 | `Ctrl + C` | `Cmd + C` |
| 粘贴 | `Ctrl + V` | `Cmd + V` |
| 撤销 | `Ctrl + Z` | `Cmd + Z` |
| 重做 | `Ctrl + Shift + Z` | `Cmd + Shift + Z` |
| 删除 | `Delete` / `Backspace` | `Delete` / `Backspace` |
| 重置视图 | `Ctrl + 0` | `Cmd + 0` |
| 打开设置 | `Ctrl + ,` | `Cmd + ,` |
| 取消选择 | `Esc` | `Esc` |

---

## 结语

恭喜你！现在你已经掌握了无限画布的所有基础和高级功能。

**开始创作吧！** 🎨

记住几个关键点：
1. 先配置好 API 接口
2. 使用提示词库快速起步
3. 善用节点连线组织创作流程
4. 定期导出备份重要项目
5. 遇到问题查看常见问题或联系社区

祝你在 AI 创作的旅程中玩得开心！ 🚀

---

**本教程版本**：v1.0
**适用于**：无限画布 v0.16.0
**最后更新**：2026-08-19
